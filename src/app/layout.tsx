import React from 'react';
import type { Metadata, Viewport } from 'next';
import '../styles/tailwind.css';

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
};

export const metadata: Metadata = {
  title: 'OmicsMind — Think Like a Bioinformatician. Reason with AI.',
  description:
    'Navigate real omics decisions across bacterial WGS, tumor somatic variants, and metagenomics scenarios. Stream live AI reasoning from Claude. Built by GenoFlow Agency.',
  icons: {
    icon: [{ url: '/favicon.ico', type: 'image/x-icon' }],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
